
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nfs-dyn-5m-pvc

spec:
  storageClassName: nfs-client-retained
  accessModes:
    - ReadWriteMany

  resources:
    requests:
      storage: 5Mi